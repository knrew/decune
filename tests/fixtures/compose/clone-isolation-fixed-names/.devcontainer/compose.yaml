services:
  app:
    image: alpine:3.20
    container_name: fixed-app-385
    command: sleep infinity
    networks:
      - appnet
    volumes:
      - cache:/cache
      - ../..:/workspace
  probe:
    image: alpine:3.20
    command: sleep infinity
    networks:
      - appnet

networks:
  appnet: {}

volumes:
  cache:
    name: fixed-cache-385
