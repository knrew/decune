[package]
authors     = ["knrew"]
description = "Linux container-side tools for decune"
edition     = "2024"
license     = "MIT"
name        = "decune-container-tools"
publish     = false
version     = "0.1.0"

[dependencies]
anyhow = "1.0.102"
decune-container-protocol = {path = "../decune-container-protocol"}
serde_json = "1.0.149"
tokio = {features = ["io-util", "net", "rt-multi-thread"], version = "1.52.3"}
